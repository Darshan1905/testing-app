import 'package:flutter/material.dart';

class EditProfilePhoneWidget extends StatelessWidget {
  const EditProfilePhoneWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
